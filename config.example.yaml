panel:
  password: "改我"
  port: 8790
anymail:
  api_key: ""
  base_url: ""
  domain: ""
  expires_hours: 0  # 0=永久（默认）；正数=限时小时数，到期邮箱被 AnyMail 清理
register:
  login_timeout: 120
  auto_login: true
  code_regex: ""
